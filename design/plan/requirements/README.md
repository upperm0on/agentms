# Requirements Design Alignment

Design must follow:

- MVP scope.
- Stable decisions.
- Permissions matrix.
- Non-functional requirements.

Every page spec in `design/pages/` maps to a planned route and avoids excluded capabilities such as bookings, payments, marketplace, tenant management, and in-app chat.

